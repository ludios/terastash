#!/bin/bash

# Enable "long stack traces" on bluebird promises
export BLUEBIRD_DEBUG=1

SELF="$(dirname "${BASH_SOURCE[0]}")"
# On Windows, node needs a normal Windows path, not a cygwin path
if [ -f /usr/bin/cygpath ]; then
	SELF="$(/usr/bin/cygpath -w "$SELF")"
fi
node --strong-mode --harmony-rest-parameters "$SELF/ts.js" "$@"
